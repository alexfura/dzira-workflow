# api/fixtures/states.yaml

App\Entity\State:
  next_states_{1..5}:
    title: <text(10)>
    description: <text(20)>

  previous_states_{1..5}:
    title: <text(10)>
    description: <text(20)>

  states_:
    title: <text(10)>
    description: <text(20)>
    nextStates: '@next_states_{1..5}'
    previousStates: '@previous_states_{1..5}'